const printMyName = (name, age) => {
  console.log(name, age);
};

printMyName("Konrad", 19);

const multiply = (number) => {
  return number * 2;
};

console.log(multiply(2));

const multiply2 = (number) => number * 2;

console.log(multiply(4));
